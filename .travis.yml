language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t eppifoss/eureka -f Dockerfile.dev   . 

script:
  - docker run  -e CI=true eppifoss/eureka npm run test

deploy:
  provider: elasticbeanstalk
  region: "us-west-2"
  app: "eureka"
  env: "eureka-env"
  bucket_name: "eureka-bucket-aws"
  bucket_path: "eureka"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key:
    secure: "$AWS_SECRET_KEY"    


